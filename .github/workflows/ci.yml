name: CI

on:
  push: {}
  pull_request:
    branches: [main, develop]

env:
  CARGO_TERM_COLOR: always
  TARGET: thumbv6m-none-eabi

jobs:
  check:
    name: Format + Lint
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6

      - name: Set version from tag
        if: startsWith(github.ref, 'refs/tags/v')
        run: printf '%s' "${GITHUB_REF_NAME#v}" > VERSION

      - uses: dtolnay/rust-toolchain@stable
        with:
          targets: thumbv6m-none-eabi
          components: rustfmt, clippy

      - uses: astral-sh/setup-uv@v6

      - uses: actions/setup-node@v6
        with:
          node-version: '20'

      - name: Install system dependencies
        run: sudo apt-get update && sudo apt-get install -y libudev-dev

      - name: Check formatting
        run: cargo fmt --all -- --check

      - name: Lint (Rust + Python + Markdown)
        run: make lint

  test:
    name: Unit Tests
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6

      - name: Set version from tag
        if: startsWith(github.ref, 'refs/tags/v')
        run: printf '%s' "${GITHUB_REF_NAME#v}" > VERSION

      - uses: dtolnay/rust-toolchain@stable

      - uses: astral-sh/setup-uv@v6

      - name: Run tests
        run: make test-unit

  build:
    name: Build All
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6

      - name: Set version from tag
        if: startsWith(github.ref, 'refs/tags/v')
        run: printf '%s' "${GITHUB_REF_NAME#v}" > VERSION

      - uses: dtolnay/rust-toolchain@stable
        with:
          targets: thumbv6m-none-eabi, x86_64-pc-windows-gnu
          components: llvm-tools-preview

      - name: Install tools
        run: |
          cargo install cargo-binutils flip-link
          sudo apt-get update && sudo apt-get install -y libudev-dev gcc-arm-none-eabi cmake mingw-w64

      - name: Build all
        run: make all

      - name: Upload artifacts
        uses: actions/upload-artifact@v7
        with:
          name: build
          path: |
            target/${{ env.TARGET }}/release/crispy-bootloader
            target/${{ env.TARGET }}/release/crispy-bootloader.bin
            target/${{ env.TARGET }}/release/crispy-bootloader.uf2
            target/${{ env.TARGET }}/release/crispy-fw-sample-rs
            target/${{ env.TARGET }}/release/crispy-fw-sample-rs.bin
            crispy-fw-sample-cpp/build/crispy-fw-sample-cpp.elf
            crispy-fw-sample-cpp/build/crispy-fw-sample-cpp.bin
            target/release/crispy-upload
            target/x86_64-pc-windows-gnu/release/crispy-upload.exe

  sbom:
    name: Generate SBOM
    # SBOMs on PR/push are produced by security-scan.yml.
    # Here we only need them to bundle into the GitHub Release on tag push.
    if: startsWith(github.ref, 'refs/tags/v')
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6

      - name: Set version from tag
        if: startsWith(github.ref, 'refs/tags/v')
        run: printf '%s' "${GITHUB_REF_NAME#v}" > VERSION

      - uses: dtolnay/rust-toolchain@stable
        with:
          targets: thumbv6m-none-eabi

      - uses: astral-sh/setup-uv@v6

      - uses: actions/setup-python@v6
        with:
          python-version: '3.11'

      - name: Install SBOM tools
        run: |
          cargo install cargo-cyclonedx --locked
          pip install cyclonedx-bom

      - name: Generate SBOMs
        run: make sbom SBOM_OUT=sbom

      - name: Upload SBOM artifacts
        uses: actions/upload-artifact@v7
        with:
          name: sbom
          path: sbom/*.cdx.json

  release:
    name: Create Release
    needs: [check, test, build, sbom]
    if: startsWith(github.ref, 'refs/tags/v')
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v6

      - name: Set version from tag
        run: printf '%s' "${GITHUB_REF_NAME#v}" > VERSION

      - name: Resolve release channel from tag
        id: release_channel
        shell: bash
        run: |
          tag="${GITHUB_REF_NAME}"
          if [[ ! "${tag}" =~ ^v([0-9]+)\.([0-9]+)\.([0-9]+)$ ]]; then
            echo "Tag '${tag}' must follow vX.Y.Z format." >&2
            exit 1
          fi

          minor="${BASH_REMATCH[2]}"
          # Official releases are only for even, non-zero minor versions.
          if (( minor % 2 == 0 && minor != 0 )); then
            echo "prerelease=false" >> "${GITHUB_OUTPUT}"
            echo "make_latest=true" >> "${GITHUB_OUTPUT}"
          else
            echo "prerelease=true" >> "${GITHUB_OUTPUT}"
            echo "make_latest=false" >> "${GITHUB_OUTPUT}"
          fi

      - name: Download all artifacts
        uses: actions/download-artifact@v8
        with:
          path: artifacts

      - name: Prepare release assets
        run: ./scripts/ci/prepare-release-assets.sh release release

      - name: Create Release
        uses: softprops/action-gh-release@v2
        with:
          files: release/*
          generate_release_notes: true
          prerelease: ${{ steps.release_channel.outputs.prerelease }}
          make_latest: ${{ steps.release_channel.outputs.make_latest }}
